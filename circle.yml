machine:
  timezone:
    America/New_York
dependencies:
  override:
    - yarn
test:
  override:
    - yarn test
deployment:
  master:
    branch: homePage
    commands:
      - yarn run build
      - aws s3 sync build s3://${BUCKET_NAME}/