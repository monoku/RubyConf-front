machine:
  timezone:
    America/New_York
dependencies:
  commands:
    - npm install yarn
test:
  commands:
    - yarn test
deployment:
  master:
    branch: homePage
    commands:
      - yarn run build
      - aws s3 sync build s3://${BUCKET_NAME}/